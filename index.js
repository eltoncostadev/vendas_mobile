/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navegador from './src/Navegador';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navegador);
